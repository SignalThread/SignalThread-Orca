/** Cookie: validated server-side; URL `eventId` takes precedence when present and accessible. */
export const EXHIBITOR_APP_ACTIVE_EVENT_COOKIE = "leadintel_exhibitor_app_active_event_id";

export const EXHIBITOR_APP_ACTIVE_EVENT_COOKIE_MAX_AGE_SEC = 60 * 60 * 24 * 365;
