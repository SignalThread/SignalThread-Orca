# OrcaOS Slice 2A — Import Workflow Audit

**Status:** complete; source audit reconciled from independent route/service, authorization, persistence, UI, and test-coverage reviews.
**Scope:** current `feature-updates-initial-demos` at `81069cdb`. No schema or application changes were made during this audit.

## Audit gate

- The branch is `feature-updates-initial-demos`, ahead of origin by the three supplied Slice 1 commits (`6cbe37af`, `ee674d8d`, `81069cdb`).
- `git status --short --branch`, `git diff --name-only`, and untracked-file inspection were clean before the audit. Slice 1 is therefore complete and does not conflict with this slice.
- Reviewed: `ENGINEERING_STANDARDS.md`, project/architecture/schema/RBAC/testing docs, Slice 0, dependency map, autonomous progress, database review, Slice 1 trust audit and QA checkpoint, and the existing Event Import Builder plan and prompt pack.
- Database classification: **(1)** no database change, **(2)** existing model safely supports it, **(3)** additive schema may be required, **(4)** existing data/migration risk requires Sarah’s review.

## Observed workflows

| Workflow | Exact surface | Input, preview, mapping, approval | Persistence / failure / duplicate behavior | Auth, isolation, UI, tests | Finding / smallest safe improvement | DB class |
|---|---|---|---|---|---|---|
| New event builder | `events/_components/new-event-builder.tsx`; `POST /api/events/import/create`; `createEventFromImportPlan` | CSV/XLS/XLSX workbook, pasted agenda, code template, or blank. Local sheet mapping/validation and explicit Review/Create. | One transaction creates Event, EventMember/defaults, rooms, MatrixRows, speakers/assignments, Budget/line items, Timeline/dependencies; rollback is atomic. A successful replay creates a second workspace. | Current route passes only id/org to the service, so MEMBER/VIEWER can create an event and become EVENT_ADMIN. Internal only. Responsive mapping tables intentionally scroll. Tests: `event-import-builder*.test.ts`. | **P1:** enforce existing OWNER/ADMIN/SUPER_ADMIN create capability server-side; add one neutral EventActivity IMPORTED summary. Durable replay prevention is deferred. | Auth/audit: 1. Idempotency: 4. |
| Run of Show / Matrix | `matrix-import-action.tsx`; `POST /api/events/:eventId/matrix-rows/import`; `validateMatrixImportRows`, `importMatrixRows` | CSV/XLS/XLSX; one selected sheet, local map/validation/paginated preview, explicit import. | Valid rows bulk-create atomically with activity; replay appends duplicates. F&B-like values remain notes only. | Route and service assert write access and stamp `eventId`; no public path. No parsing state. Tests: `matrix-import.test.ts`. | **P2:** parsing/loading lock and aria-live status. Durable idempotency is deferred. | UI: 1. Retry identity: 3/4. |
| Budget | `budget-import-action.tsx`; `POST /api/events/:eventId/budget/import`; `validateBudgetImportRows`, `importLineItems` | CSV/XLS/XLSX, mapping, validation, 15-row preview, explicit import; valid rows apply and invalid rows are reported/skipped. | Line items/activity atomic; group resolution can precede the line-item transaction. Replay duplicates line items. | Route has write guard, but exported `importLineItems` itself has no access argument/assertion. Tests: mapping/write plus one DB group journey. | **P1:** require existing `BudgetAccessUser` in service and assert write access. **P2:** parsing state. Leave group/retry semantics documented pending decision. | Guard/UI: 1. Retry/group policy: 4. |
| Timeline | `timeline/page.tsx`; `POST /api/events/:eventId/timeline-items/import`; `validateTimelineImportRows`, `importTimelineItems` | CSV/XLS/XLSX, mapping, local validation/preview, explicit import. | Atomic chunked write/activity; replay duplicates. | Route/service assert write and event-stamp rows. Preview truncates at 50 silently. Tests: `timeline-import.test.ts`. | **P2:** disclose “showing first 50 of N; all valid rows import” and parse loading state. | UI: 1. Retry identity: 3/4. |
| Attendee / registration | `attendee-import-modal.tsx`; `POST /api/events/:eventId/attendees/imports`; `parseAttendeeImportRow`, `processAttendeeCsvImport` | CSV/XLS/XLSX, auto-map and local preview, explicit import. | Sequential per-row person/role/attendee/registration write; returns response-local row outcomes, no batch history. Re-import updates by event-local email; interruption can partially apply. | Service write guard and event-scoped ownership fields. UI silently chooses first XLSX sheet and retains prior valid draft when a new parse fails. Tests: `event-attendee-import.test.ts`. | **P1:** clear draft before parse and reject multi-sheet workbooks clearly. Do not add a post-write activity summary: it cannot be atomic with the existing sequential import. | UI: 1. Durable outcomes/audit: 3/4. |
| Directory / partner-like contacts | `directory-import-modal.tsx`; `POST /api/events/:eventId/directory/imports`; `processDirectoryCsvImport` | CSV/XLS/XLSX mapping/preview and explicit import, selected directory role. | Existing batch/row history; normalized-email update, no-email collision review, no deletion. Per-row processing may leave `PROCESSING` on unexpected failure; no durable replay identity. | Service guards reads/writes and verifies batch event match. Same first-sheet/stale-draft UI issue. Tests: `event-directory-import.test.ts`, route/UI regressions. | **P1:** clear/reject multi-sheet UI; safely mark existing batch FAILED on unexpected processing failure only if verified against current enum/model. Email uniqueness/resume needs review. | UI: 1. Failure status: 2. Identity/uniqueness: 4. |
| Speaker | `speaker-csv-import-modal.tsx`; `POST /api/events/:eventId/speakers/import`; `importSpeakersFromMappedRows` | CSV only; column mapping and samples, explicit import. | Sequential writes; request-local email/name de-dup and response summary; partial results are possible. | Service write guard; event-local queries. No row preview. Existing free-text `dietaryRestrictions` is out of bounds. Tests: `speaker-bulk-tools-regression.test.ts`. | **P2:** show bounded mapped-row preview and invalid/skipped explanation. Concurrent canonical duplicate policy stays deferred. | UI: 1. Uniqueness: 4. |
| Marketing audience | `marketing/_components/audience-import-modal.tsx`; recipient route; `importRecipients` | CSV mapping and explicit import into/new audience. | Per-row create with DB uniqueness handling, recipient-count update and Marketing activity; partial row failures return in summary. | Service resolves the audience then asserts event write access. Route does not compare path `eventId` to audience event; service scope is authoritative. UI parsing/submission lock and narrow table handling are weak. Tests: `marketing-service-regression.test.ts`. | **P2:** loading/close lock and responsive mapping table. **P2 security contract:** pass/verify eventId in route/service so a path cannot target a different event; test it. | 1 |
| Documents/specifications | Docs Hub upload/finalize/review surfaces, not a mapped import executor. | Generic binary upload, no CSV/XLSX/PDF mapping preview. | Retry survives only while dialog remains mounted; durable outcome is not present. | Internal event-scoped document routes. | **P1:** prevent dismissal while multipart upload is active. Durable recovery remains TA-09. | UI: 1. Durability: 4. |
| Rooming/housing | None found. | — | — | — | Record absence; do not invent workflow/storage. | — |
| F&B/menu PDF ingestion | F&B catalog parse-menu routes and source-menu services. | PDF/R2 parse, not a Slice 2 import surface. | Persists menu/catalog data. | Explicitly out of scope. | No change: menu, dietary, allergens, verification, coverage, and session-to-menu relationships are deferred. | Deferred |

## Reconciled defects and recommended Slice 2B scope

Implement only: event-builder role enforcement and neutral import activity; budget service access assertion; attendee activity; attendee/directory stale-draft reset plus multi-sheet rejection; Matrix/Budget/Timeline parse-state lock; Timeline preview disclosure; bounded speaker preview; marketing parse/submit/responsive hardening and route event-id match; docs upload dismissal lock; focused regressions.

Do not implement: durable idempotency, generalized import history, replay recovery, durable import outcomes, speaker/directory uniqueness constraints, attendee batch storage, rooming/housing, document/spec importer, or any F&B-related persistence/design.

## Test gaps

Most existing coverage is pure mapping or source-contract coverage. Add targeted tests for every changed UI/service path, event-builder MEMBER/VIEWER denial, direct budget-service access denial, imported EventActivity summaries, attendee/directory stale-draft and multi-sheet rejection, and route event mismatch for marketing. DB-backed route/isolation/replay/partial-failure suites remain follow-up work; tests must document current non-idempotent semantics rather than assert an unimplemented guarantee.
