# OrcaOS Slice 3A — Matrix/session persistence audit

**Status:** audit complete; implementation is partially blocked by a persisted staff-assignment authority conflict. The audit itself made no application, schema, migration, seed, index, relation, or persisted-data change; it was committed with the independent no-schema Slice 3B work in `3661c75d`.

## Audit gate and method

- **Branch/worktree:** `feature-updates-initial-demos`, at `b6fd1fe6`; no staged, modified, or untracked files at audit start. `docs/product/` has no untracked or changed files, so it does not conflict with Slice 3.
- **Documentation read:** `ENGINEERING_STANDARDS.md`, `docs/PROJECT_CONTEXT.md`, `docs/SYSTEM_ARCHITECTURE.json`, `docs/RBAC_MATRIX.json`, `docs/SCHEMA_GUARDRAILS.md`, `data/schema.md`, testing harness/coverage/regression-policy documents, Slice 0 baseline, all Slice 1 and Slice 2 audit/QA/progress documents, the dependency map, autonomous progress, and database review. No current Slice 3 prompt pack exists in the repository; the approved user scope is controlling.
- **Independent reviews:** route/service inventory, authorization/event isolation, and persistence/UI/test coverage reviews reconciled below. Reviewers made no changes.
- **Terminology:** the dependency map labels R08–R13 as Slice 2 while the approved execution prompt calls this Slice 3; this artifact uses the approved Slice 3 naming.

## Classification key

1. No database change required.
2. Existing model safely supports the fix.
3. Additive schema change may be required.
4. Product or database decision required.

## Workflow inventory and findings

| Workflow / R | Route, component, service, model | Current behavior and source of truth | Validation, authorization, isolation | Persistence, reload, duplicate/delete/stale behavior | UI and tests | Class / smallest safe action |
|---|---|---|---|---|---|---|
| Session create, edit, duplicate, delete (R08) | Legacy routes `api/events/[eventId]/matrix-rows/{route,[rowId]/route,[rowId]/duplicate/route}`; `matrix.ts:createMatrixRow/updateMatrixRow/duplicateMatrixRow/deleteMatrixRow`; `MatrixRow`, `Room` | `MatrixRow` is canonical. Create accepts date/name/room/time/attendance/etc.; edit uses explicit partial fields; duplicate intentionally creates a new MatrixRow; delete is transactional and writes activity. | Routes resolve request user and require event write/read. Services find MatrixRow/Room by `id,eventId`; date/time/attendance and room ownership are validated. | Legacy list and Matrix 2 snapshot reread MatrixRow. Partial edits preserve omitted fields. Intentional duplicate and retry can create multiple rows; no durable request identity. Delete verifies scoped row, but client stale views require reread. | `matrix2-add-session-regression`, `matrix-rows-auth-regression`, `matrix-rows-partial-update-regression`, room-flow tests. Missing: DB route-level denied-user and failed-delete reload journeys. | **2** for core existing-model hardening; retry/idempotency is **4** and paused. |
| Matrix 2 basics and status (R08/R09) | `api/.../matrix-2/route`, `api/.../sessions/[sessionId]/route`; `matrix2.ts:getMatrix2Snapshot`; `matrix2-session.ts:updateMatrix2Session`; `matrix-2/page.tsx`; `Matrix2DetailsDrawer`; `session-status.ts`; `MatrixRow.notes` | Snapshot reads event-scoped MatrixRow and derives type/status from structured notes. PATCH partially merges title/type/time/room/attendance/setup/status and persists MatrixRow in a transaction. Status choices come from active event STATUS template items. | Snapshot requires event read; PATCH requires event write before body validation or mutation. Service scopes MatrixRow, Room, Speaker, and EventPerson to `eventId`; validates required title, HH:mm, end-after-start, non-negative integer attendance, canonical status, and event room. The route allowlists nonempty object inputs; service itself has no actor/access parameter. | Omitted fields are preserved, explicit empty arrays clear related data, and the UI reloads snapshot after save. Global snapshot in-flight cache is keyed only by event ID, but authorization occurs before awaiting it and snapshot content is not user-specific. Status remains denormalized in notes. | `matrix2-session-type-persistence`, `matrix2-session-partial-merge`, `session-status`, `session-readiness`, `matrix2-snapshot-parity`, event-routing, and retry/input regressions. Missing: route-level title/time/room/status reload and failed-save recovery DB journeys. | Core data path **2**; unknown-key/empty request hardening **1** is complete. See staff coupling blocker below before any Matrix 2 UI save change. |
| Speakers (R11, allowed subset) | `api/.../sessions/[sessionId]/speakers/[speakerId]`; `matrix2-session.ts:add/removeMatrix2SessionSpeakerAssignment`; `SessionSpeakerAssignment` | Canonical assignment model is schema-backed `SessionSpeakerAssignment` with composite uniqueness. PATCH can also replace speaker selection. | Routes require event write. Service scopes session and speaker by event; exact-name selection rejects ambiguous speaker matches. | Dedicated add/remove reread via snapshot. Replacement delete/reinsert is transactional; duplicate assignments conflict-do-nothing. No speaker identity/concurrency policy is introduced here. | Partial-merge journey, speaker access regression, and quick-drawer browser journey exist. Missing DB route/isolation/reload coverage for add/remove. | **2** for focused existing-model route/test hardening; speaker uniqueness/concurrency remains **4**, paused. |
| Staff assignments (R11) | `matrix2.ts:listMatrixRowStaffAssignmentsCompat`; `matrix2-session.ts:updateMatrix2Session`; Prisma `SessionStaffAssignment`; legacy raw table `MatrixRowStaffAssignment` | Prisma and migration `20260313120000_matrix2_session_operations` define `SessionStaffAssignment(sessionId,personId,role)`. Runtime raw SQL instead reads/writes `MatrixRowStaffAssignment(matrixRowId,eventPersonId,assignmentrole)`, which neither schema represents nor that migration creates. | Current routes have event write guard and EventPerson lookup is event-scoped, but neither guard resolves conflicting persisted authority. | A migration-conformant DB can fail staff read/write; a legacy DB can hide `SessionStaffAssignment` rows from Matrix 2 reload. Matrix 2 drawer and full workspace always include `staffAssignments`, so Basics/status saves can invoke the raw staff rewrite even when staff are untouched. | Existing snapshot-parity/partial-merge journeys use schema-shape fixtures but do not prove the runtime raw-table contract. No trustworthy staff reload/isolation contract exists. | **4. Stop.** Do not workaround by silently omitting staff or dual-writing. Sarah must choose authority/reconciliation; database review section 8 is updated. |
| AV requirements (R11, allowed subset) | `matrix2-session.ts` `avRequirements`; `matrix2.ts` snapshot; `SessionAVRequirement`; `MatrixRow.avNeeds` | Runtime serializes AV requirements into legacy `MatrixRow.avNeeds` and reconstructs pseudo entries on snapshot; it does not use the schema `SessionAVRequirement` model. | Matrix 2 PATCH guard and event session scope apply. Quantity is positive integer. | Partial merge preserves omitted AV legacy text; explicit input overwrites text. A model-authority migration/refactor could make existing data invisible. | Partial-merge coverage is legacy-path oriented. No canonical AV model reload/isolation journey. | **4** for source-of-truth migration/refactor; no AV persistence change in this slice. |
| F&B / menus / dietary (R11) | Matrix 2 F&B plan/catalog-assignment routes; SessionFoodService/F&B catalog models | Existing but explicitly excluded. | Route guards were observed but are not a basis for new work. | F&B behavior must not be altered or used to close R11. | Existing F&B tests intentionally not treated as Slice 3 acceptance. | **Paused by scope.** |
| Requirements and readiness (R09/R11) | requirement routes, `session-requirements.ts`, `session-readiness.ts`, template/selection models | Readiness is derived from MatrixRow plus requirement selections; template STATUS values are authoritative. Snapshot GET ensures a requirement template, which can write during a read. | Requirement mutation routes use event write guard; selections are scoped by event/session. | Derived readiness reloads from snapshot. The GET-time template creation is hidden persistence and must not be changed incidentally. | `session-readiness`, `session-requirement-selection-persistence`, status-card tests. Missing route-level error/retry and permission journeys. | Existing selection work **2**; changing GET-write contract **4**. |
| Board/list and errors | `matrix-2/page.tsx:loadSnapshot`, Matrix 2 board/list components | Both views consume snapshot response and stale snapshot response is discarded. | API requires event read. | Save reloads canonical snapshot. Failed initial load clears data and displays an error banner with Retry; mutation failures remain distinct. | Focused retry and event-routing regressions cover the initial-load path; browser error/retry coverage remains absent. | Retry UI + focused test **1** is complete; full Matrix 2 save contract remains blocked by staff coupling. |
| Full session workspace errors | `matrix/sessions/[sessionId]/_components/session-detail-workspace.tsx:loadSnapshot` | Workspace loads Matrix snapshot and reloads after save. | Uses scoped event/session API paths. | Initial fetch error clears state and offers Retry. Request versions discard stale responses, and a snapshot whose event differs from the route is rejected before state update. Saves include staffAssignments and therefore reach the blocked raw staff path. | Responsive utility classes and focused stale/error/retry source regressions exist; no browser proof for initial-error retry. | Retry/stale presentation **1** is complete; save-path work is **4** due staff coupling. |
| Room Set and Seating linkage (R13) | room-set workspace; `seating.ts:ensureSeatingPlanForMatrixRow/resolveSeatingWritePlanId`; seating routes; `SeatingPlan.matrixRowId` | Existing session-scoped SeatingPlan and Room Set links are authoritative. Session workspace uses matrixRowId and mode (`layout`/`seating`). | Routes require event access. Service validates MatrixRow `{id,eventId}`, seating plan event ownership, matching matrixRowId, table and attendee scope before writes. | Scoped seating snapshot/refetch clears stale selected state when session changes. Exact chair assignment is persisted; sibling sessions cannot modify each other. Room Set layout draft uses browser-local storage; server-backed durability is excluded. | `seating-plan-scoping`, `seating-chair-scope`, Room Set workspace and browser journeys. Failed Room Set linkage fetch only offers Return; seating subpanel has Refresh. Missing DB-backed denied-user/reload integration. | Core scope **2**; fetch Retry UI **1**; server-backed layout **4**, paused. |
| Session roster, check-in/no-show | session attendee routes/helpers; `event-attendee-session-enrollment.ts`; `EventAttendee` and `EventAttendeeSessionEnrollment` | `checkedInAt` and CHECKED_IN/NO_SHOW lifecycle already belong to attendee/enrollment records, not MatrixRow. No `processNoShows` symbol or automatic no-show route exists. | Existing roster routes require event read/write and service validates linked event-scoped attendee/session/registration/integration records. | Add/cancel lifecycle is persisted; new no-show automation would require time-policy and durable history decisions. | Enrollment regression coverage exists. | Existing roster behavior **2**; automatic no-show/check-in history **4** and explicitly paused. |
| Matrix import | `matrix-rows/import/route`, `matrix.ts:importMatrixRows` | Imports create MatrixRow records and activity; service asserts event write internally. | Event write enforcement and input validation are present. | Partial success and retry can duplicate rows; no durable import identity/history/idempotency. | `matrix-import.test` exists. | **4**, explicitly paused. |

## Baseline validation

- **Focused Matrix/session/auth/seating suite:** 121 passing assertions, including a DB-backed Matrix 2 type-persistence test. Matrix access, route event isolation, status/readiness, requirement selection, session partial merge, snapshot parity, and seating scope tests passed.
- **Known failing Room Set linkage source-contract suite:** 7 of 14 tests fail in `web/lib/run-of-show-room-set-linkage-regression.test.ts`. The assertions expect retired source markers/actions (`const overviewItem`, prior board/quick-action markup) that current UI no longer contains. The failure occurred before any audit change and is not attributable to Slice 3. Do not amend production behavior merely to satisfy stale source-shape expectations; triage/update these tests in a separate approved no-schema QA task.
- **Typecheck:** existing stale `.next` validators reference absent AI Workspace paths.
- **Lint:** existing 68 errors and 83 warnings, including pre-existing Matrix/session-area warnings; no audit code changed.
- **Build:** previously passed at `b6fd1fe6`; rerun is practical only after implementation because this audit changed documentation alone.
- **`git diff --check`:** must pass before audit handoff.

## Database and product gates

1. **Staff assignment authority (class 4):** choose (A) schema/migration `SessionStaffAssignment` with approved legacy reconciliation, (B) migrate legacy rows then retire compatibility access, or (C) declare `MatrixRowStaffAssignment` canonical with schema/migration. Choose conflict precedence, historical disposition, and rollback. This is documented in `orca-database-review.md` section 8.
2. **AV source of truth (class 4):** decide whether legacy `MatrixRow.avNeeds` remains authoritative or whether an intentional, reconciled move to `SessionAVRequirement` is required. No current task requires this change.
3. **Requirement-template GET write (class 4):** decide whether Matrix snapshot may continue to ensure template rows during GET before altering that behavior.
4. **No-show automation/history (class 4):** define eligibility/timezone/time policy and durable audit/history implications before adding `processNoShows` or new check-in behavior.

## Recommended Slice 3B scope (historical; completed independent items noted)

Do **not** claim R11 complete or make Matrix 2 save-path changes until the staff authority decision. Once resolved, the smallest vertical implementation is:

1. Make the chosen staff record authoritative with an approved reconciliation/migration plan outside this no-schema scope.
2. **Completed in `3661c75d` and `4737613d`:** Matrix 2 PATCH allowlist and empty-payload rejection, with authorization-before-validation and focused route tests (R08/R09, class 1).
3. **Completed for Run of Show and full session workspace:** truthful retry controls and stale/mismatched snapshot rejection, with focused tests (class 1). Room Set remains unchanged.
4. Add DB-backed route isolation/reload journeys for title/time/room/status, speaker assignment, and session-scoped seating (R08/R09/R11 speaker/R13).

F&B, dietary, menu, allergen, verification, coverage, session-to-menu architecture, import replay/idempotency/history, speaker concurrency, durable document outcomes, server-backed command-center persistence, and new check-in history remain intentionally deferred.
